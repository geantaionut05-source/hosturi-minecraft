<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Planuri Minehant</title>
  <link rel="stylesheet" href="style.css"> <!-- Folosește același CSS ca pagina principală -->
  <style>
    .plans {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 16px;
      margin-top: 16px;
    }
    .plan {
      background: linear-gradient(180deg, #0f1511, #0e1210);
      border: 1px solid rgba(255,255,255,0.03);
      padding: 18px;
      border-radius: 10px;
      text-align: center;
    }
    .plan h3 { margin: 6px 0 8px; font-size: 18px }
    .price { font-size: 22px; color: var(--accent); font-weight: 700; margin: 6px 0 }
    .specs { color: var(--muted); margin: 10px 0 14px }
    .btn {
      display: inline-block;
      padding: 10px 16px;
      border-radius: 8px;
      background: var(--accent);
      color: #03120b;
      text-decoration: none;
      font-weight: 700;
      box-shadow: 0 6px 18px rgba(0,204,119,0.12);
    }
  </style>
</head>
<body>

  <header>
    <div class="brand">
      <img src="minehant-logo.png" alt="Minehant logo" onerror="this.style.display='none'">
      <div>
        <h1>Minehant</h1>
        <p class="lead">Planuri gratuite 1GB - 5000GB</p>
      </div>
    </div>
  </header>

  <main class="wrap">
    <h2>Toate planurile gratuite</h2>
    <div class="plans" id="plansContainer"></div>
  </main>

  <footer>
    © Minehant • Toate planurile sunt demonstrative.
  </footer>

  <script>
    const plansContainer = document.getElementById('plansContainer');
    for (let gb = 1; gb <= 5000; gb++) {
      const plan = document.createElement('div');
      plan.className = 'plan';
      plan.innerHTML = `
        <h3>Plan ${gb}GB</h3>
        <div class="price">0€ / lună</div>
        <div class="specs">${gb} GB RAM • ${gb} GB stocare (SSD)</div>
        <a class="btn" href="#">Creează server</a>
      `;
      plansContainer.appendChild(plan);
    }
  </script>

</body>
</html>
